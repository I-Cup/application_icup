<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Application_icup" parent="Theme.Design.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- fonts -->
    <style name="text_style">
        <item name="android:fontFamily">@font/code_pro_bold</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">@dimen/default_textSize</item>
    </style>

    <style name="subtitle_style" parent="text_style">
        <item name="android:textColor">@color/title_color</item>
    </style>

    <style name="title_style" parent="subtitle_style">
        <item name="android:textSize">@dimen/default_titleSize</item>
    </style>

    <style name="identity_text_style" parent="title_style">
        <item name="android:textColor">@color/identity_color</item>
        <item name="android:textSize">@dimen/default_textSize</item>
    </style>

    <!-- boxes -->
    <style name="default_box">
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:background">@color/identity_color</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:layout_marginTop">@dimen/default_margin</item>
    </style>




</resources>